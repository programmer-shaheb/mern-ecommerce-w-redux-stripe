import express from "express";

const router = express.Router();

router.put("/:id", () => {});
router.delete("/:id", () => {});
router.get("/find/:id", () => {});
router.get("/", () => {});
router.get("/stats", () => {});

export default router;
